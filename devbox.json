{
  "packages": [
    "elan@latest"
  ],
  "shell": {
    "init_hook": [
      "git config core.hooksPath .githooks"
    ],
    "scripts": {
      "build": "lake build",
      "test": "lake test",
      "check": "lake build && lake test"
    }
  }
}
